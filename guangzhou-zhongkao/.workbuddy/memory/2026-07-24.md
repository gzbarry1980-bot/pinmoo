# 2026-07-24 工作日志

## 广州中考志愿模拟系统 · 架构/流程优化 + UI美化
- 走标准 SOP 增量优化（架构师→工程师→QA），严守 engine.js 与 data/ 业务逻辑禁区不动。
- 关键边界：engine.js（388行/22208字节/SHA256 fc8f643c…bfbebf60）含全部投档/录取判断（evaluateRecord/replayPlan/simulateOutcomes/evaluatePlan/gradientIndex 等）；data/*.json 为官方数据集。app.js 对 engine 的调用点（seed 20260722 主/方向、20260817 目标校、iterations 10000/600/350|800|1200）须原样保留。
- 交付物（docs/）：system_design.md（设计+边界+任务分解 T01–T05）、class-diagram.mermaid、sequence-diagram.mermaid、qa-check.md；新增 qa/verify-engine.mjs（确定性抽样回归脚本）、qa/regression-report-qa.md（QA报告）。
- 落地的优化：T01 设计令牌层(:root)+统一 .btn/.card/.field 基类；T02 导航收敛（移除 verify 版冗余 compact-back、guideStart 改"下一步：X"、navigation.js 锚点失效修复）；T03 核心模块视觉（danger按钮层级、阴影统一、焦点环统一、间距/圆角/字号尺度化）；T04 子页(special/unlock)token 打通；T05 流程顺滑（adopt 复用 directionAnalysis 免重算、schoolExplorer 常驻"已选 N 所"概览卡）。
- 回归结果：engine.js 校验和逐位一致、调用点守恒、确定性抽样 sig 一致、node --check 全过、建议函数结果集未变；QA 路由 NoOne，业务逻辑问题 0。
- 观察项：data/first-batch-2026.json 仅被 special/app.js 展示"第一批录取结果"用，engine.js 不加载，不影响录取判断；非本次任务新增，待用户确认是否保留/清理。
- 本地预览：python -m http.server 8000（项目根目录）。

## 新增极简首屏 landing
- 需求：用户反馈首页（原 intro-strip 大 hero + 5按钮 + 3 tab + 默认展开的 verify 四步表单）太复杂，希望"打开就3个入口卡"。已与用户确认方案：内嵌首屏 + 次级入口折叠到"更多工具"。
- 落地：index.html 用 #landing 替换 intro-strip+workspace-switch，3张大卡片(landingDirection/landingTarget/landingVerify)驱动 setWorkspaceMode；verifyWorkspace 改默认 hidden；global-nav(#homeLink) 与 quick-return(#homeReturn) 增加返回 landing（setWorkspaceMode('home')，preventDefault 避免整页刷新）。app.js：setWorkspaceMode 新增 'home' 模式、移除原 tab 点击绑定与 #analyzeTop 顶部入口（#analyzeTop 残留引用用局部变量判空守卫——可选链 `?.` 不能用于赋值左侧，会语法报错）；init 默认进 'home'（pendingDraft 时仍进 verify 以便恢复草稿）。styles.css 新增 .landing/.landing-cards/.landing-card(含--featured 推荐态)/.landing-more/.landing-previews 等，复用 T01 token。
- 约束守住：engine.js 校验和仍 == fc8f643c…bfbebf60、调用点(seed/iterations/import)未变、node --check 全过；业务逻辑零改动。
- 备注：本次 Agent 调度通道故障（子代理 history 初始化报错，TeamCreate/Agent 均失败），由主理人直接落地，自检严谨度等同 QA。

## 首页极致极简化（二次迭代）
- 需求：用户要求移除首页顶部 Banner 区域、不展示任何横幅内容；"更多工具"入口隐藏到内页展示，首页仅保留最核心元素，追求极简主义。
- 落地：index.html 删除 .landing-hero（eyebrow/h1/副文/intro-trust 信任标签）与首页内的 .landing-more（更多工具），#landing 仅留 3 张入口卡；新增 #innerTools（details 折叠，含 看示例/导入方案/恢复草稿/特长生/账户解锁 + 3张效果预览），置于免责声明前、内页底部。app.js：setWorkspaceMode 在 home 时隐藏 #innerTools、进入任意 workspace 时显示（homeMode 控制）。styles.css：.landing 由蓝色渐变横幅块改为干净留白容器（去 gradient/阴影/圆角），.landing-more 由蓝底白字重设为浅色主题（var(--c-surface)/var(--c-line)/var(--c-brand)），与新首页一致；print 媒体追加 #innerTools 隐藏。
- 约束守住：engine.js 校验和仍 == fc8f643c…bfbebf60、调用点未变、node --check 全过、CSS 大括号配平（471/471）。业务逻辑零改动。

## 移除顶部导航栏 + 首页大字标题（三次迭代）
- 需求：用户要求移除顶部导航栏（topbar），首页顶部用醒目大字标题"广州中考志愿模拟助手"，下方标注"由品沐咨询提供"及数据来源说明；主体区保留 3 个引导按钮（方向版/目标校版/求证版）进入对应模块。
- 落地：index.html 删除整段 `<header class="topbar">`（品牌+global-nav+topbar-meta+数据来源按钮）。#landing 顶部新增 `.home-masthead`：h1.home-title（clamp 大字）、p.home-byline"由品沐咨询提供"、p.home-source-note（数据来源说明 + #showSources"查看数据明细"按钮）、#dataVersion（官方数据版本提示，迁入首页）。3 张引导卡保留。app.js：守卫随 topbar 移除/迁移的元素——#dataVersion（L64/L1443 改为局部变量+if 判空）、#showSources（L1409 加 if 守卫）、#homeLink（L1362 原本已有 if 守卫，现已不存在元素不受影响）。navigation.js 无直接 topbar 引用（走事件委托），未改。styles.css：删除 .topbar 主样式块（brand/global-nav/topbar-meta/status-dot），新增 .home-masthead/.home-title/.home-byline/.home-source-note/.home-data-version；删除失效的 `.topbar .text-button`。.landing-more 内 .text-button 已是 var(--c-brand)（可读）。
- 约束：engine.js 本次未打开未编辑（仅动 index.html/app.js/styles.css），调用点(seed/iterations/import)逐位守恒；因 Bash 通道临时故障未能跑 certutil/node --check，改以静态审查确认三处守卫语法合法、homeLink 守卫完好、CSS 括号配平。业务逻辑零改动。
- 备注：本次 Bash 工具与 Agent 调度通道先后出现基础设施故障（同环境瞬时问题，非代码问题）。

## 志愿表新增"冲刺/匹配/保底"概率等级标签（四次迭代）
- 需求：用户在求证版志愿表（2026年结构模拟志愿表）中，希望"冲刺/匹配/保底"标签放在每所学校分数旁，清晰标注录取概率等级；另要求新增"特控率"数据列。
- 决策："特控率"在系统全项目（含 data/）grep 无任何匹配、schools.json 无率类字段、control-lines.json 仅梯度线无特控线；data/ 为禁区不可手填。经 AskUserQuestion 确认：先只做冲稳保标签，特控率列暂缓（用户选"先只做冲稳保标签"）。
- 落地（纯展示现有 tier，零逻辑风险）：app.js 新增 `tierForSlot(batch,position)` 从 `latestAnalysis.slotResults` 取 `row.tier`；`updateSlotMeta()` 在 slot-meta 内以 `<span class="tier-tag tier-reach|tier-match|tier-safe">冲刺/匹配/保底</span>` 置于分数线(`${record.cutoffScore}分 · 分差…`)之前（改 textContent→innerHTML，tier 取自固定集合无注入风险）；`analyze()` 增设 `updateSlotMeta()` 调用，使分析后徽标即刷新。styles.css 新增 `.tier-tag` 三档样式，复用 T01 已定义的 `--tier-reach/--tier-match/--tier-safe` 及对应 soft token（冲=危险红、配=警告橙、保=成功绿）。
- 约束守住：engine.js 校验和 == fc8f643c…bfbebf60、调用点(seed/iterations/import)逐位守恒（grep 复核 L1/L233/L335/L450/L1045-1047）、node --check(engine/app/navigation)全过、CSS 大括号配平（467/467）。业务逻辑零改动。
- 待办：特控率列待用户提供真实数据（schoolId→特控率 清单）或确认用"梯度线率"近似后再补。

## 特控率数据查证（研究，未落地）
- 用户要求"从官网查特控率更新到项目"。经 WebSearch 查证关键事实（2026-07-24）：
  - **特控率 = 高考口径指标**（特殊类型招生控制线上线率），衡量某高中毕业生超特控线比例，是家长比较高中办学质量的常用参考；**不是中考录取指标**，与志愿投档规则无关。
  - **官方中考网(gzzk.gz.gov.cn)只有梯度线，无各校特控率**：2026 广州中考 公办普高线492/民办412；一梯712/二梯672/三梯632/四梯592/五梯552/六梯512（2026-07-18 公布）。
  - **官方高考特控线（可引用）**：广东2026 历史546/物理539，来源 广东省教育考试院（南方日报/人民网 2026-06-25：https://gd.people.com.cn/BIG5/n2/2026/0625/c123932-41620448.html；腾讯 https://new.qq.com/rain/a/20260624A04IT500）。
  - **各校2026特控率（媒体汇总，可引用，保留原文表述）**：华附超九成、六中突破九成五、执信破九成、广雅两校区均超九成、铁一三校区近九成、广外九成以上、协和近六成、五中物理近九成/历史近八成、真光本部超七成半、一中纯文化六成、培英云城破八成/鹤洞破六成、六十五中近五成、玉岩超八成；省实/广附/二中/七中/南武等仅给"600分以上占比"或"稳居高位"无明确特控率。来源：信息时报 2026-07-04（https://epaper.xxsb.com/html/node/2026-07/04/A2.html）、腾讯 2026-07-03（https://new.qq.com/rain/a/20260703A08LUS00）。
  - 落地约束：data/ 为禁区不可碰，须放独立补充文件（如 reference/special-control-rate.json），标"参考·不参与录取"，每条带来源链接+更新日期；未核实学校显示"暂无数据"。
- **用户决策：先不做了（本轮回退）**。以上查证结果已留存，后续若重启可直接用。

## 视觉设计优化（五次迭代 · 纯前端视觉层）
- 需求：减少纯文字堆砌；合理使用卡片/图标/色块分隔组织信息；用字号/颜色/间距对比拉开排版层次；增加留白降低信息密度；整体美观现代统一。
- 落地（仅 index.html 结构 + styles.css 样式，未动 engine.js / data/ / app.js 调用点）：
  - 图标系统：新增 `.icon-badge`（圆角软底+描边 SVG，含 --brand/--success/--warning/--info/--navy 变体）。首屏 3 卡加 `.landing-card-icon`（方向版=指南针、目标校版=靶心、求证版=盾勾，featured 卡图标反白）；首屏加 `.home-brandmark` 渐变方块品牌锚点；方向版/目标校版 hero、求证版 workflow-guide、求证版 STEP1–4 四个 panel-heading 均加图标徽标（用户/校帽/清单/柱状图/路线）。
  - 排版层次：`.step-label` 升级为胶囊标签；`.direction-hero h2` 25px、`.panel-heading h2` 21px、导语 `.direction-hero p`/`.workflow-guide p` 放大至 14px/行高1.7 并弱化色。
  - 留白：`.panel` padding 22→26/28、`.direction-workspace` 24→28、section 间距 16→22、`.direction-form`/`.profile-grid` gap 13/14→16、label gap 7→8；移动端单独回退。
  - 色块：`.direction-stat`/`.dimension-card` 改柔和渐变底；`.chance-row`/`.outcome-row` 浅底+navy 标题强化卡片感。
- 校验：CSS 大括号 505/505 配平；engine.js SHA256 == fc8f643c…bfbebf60 逐位一致；app.js 对 engine 的 import/seed/iterations 调用点 grep 守恒（L1/L233/L335/L450/L1045-1047）；业务逻辑零改动。
- 预览：后台 `python -m http.server 8000`（task Nei5X0），宿主机浏览器开 http://localhost:8000/ 可见（沙箱 curl 回环受限但不影响用户端预览）。

## 高保真模拟系统（六次迭代 · 新增独立模块，未动 engine/data）
- 需求：用户确认要做"高保真模拟"——数据/交互/边界/异常/真实波动都贴近真实；并采纳上一轮建议"求证版改策略自动重算"。硬约束不变：engine.js（录取判断核心）与 data/ 字节级不动。
- 实现策略（合规绕过禁区）：**新建独立 ES module `realistic-sim.js`**，只重写"模拟编排"层，录取判断 100% 复用 engine.js 导出的 `evaluateRecord`（import，逐字不变）。输出结构与 engine.simulateOutcomes 完全一致（{mode, slotResults[{...slot,state,chance,interval,tier,confidence,yearsWithData,notes}], outcomes, noneProbability, iterations, seed}），app.js 渲染层零改动。
- 高保真增强点：① 单分估值注入 ±12 表现方差（原 triangular 在 low==high 时退化为定值）；② 每次抽样在分位映射后叠加 ±0.8% 年际难度/cohort 抖动；③ 压线/末位志愿由固定 0.5 抛硬币改为按"最低分—末位考生分"相对位置给梯度概率；④ 异常分支（no-data/ineligible/uncertain）统计并输出 notes（如"部分年份无同口径数据/历史数据较少区间偏宽"）。
- app.js 改动（调用点守恒）：L1 保留原 engine import；新增 `import { simulateOutcomesRealistic } from './realistic-sim.js'`；新增 `useRealisticSim` 全局 + `runSimulation()` 路由（默认走 engine.simulateOutcomes，开启走 realistic）；4 处 simulateOutcomes 调用（L254/L356/L471/L1067）改为 runSimulation，seed/iterations 原值不动；求证版 `#riskPreference` 下拉改动后自动 `analyze()`（消除"改了策略结果没变"的错觉）；结果区 `#methodText` 动态加"冲刺/匹配/保底由模拟机会测算，与策略无关"提示 + 高保真开启时注明已增强。
- index.html：方向版表单与求证版 STEP1 各加一个"高保真模拟"复选框（#realisticSimDirection / #realisticSim），绑定同一开关并即时重跑（方向版 regenerate / 求证版 analyze），localStorage 持久化（pinmoo-guangzhou-zhongkao-realistic）。复用既有 `.checkbox-field` 样式，无 styles.css 改动。
- 校验：engine.js SHA256 仍 == fc8f643c…bfbebf60（逐位一致）；app.js 对 engine 的 import/seed/iterations 调用点 grep 守恒；node --check（engine/app/realistic-sim 三个模块）全过；真实数据端到端测试通过（同种子可重复=true；区间估分时华侨中学区间 5–31% vs 标准 12–22% 更宽；单分估分 650 落榜风险 29.5% vs 标准 20.6%，且不再退化为定值；自动标注无同口径数据年份）。
- 说明：真实波动幅度刻意保守（不夸大），是在 engine 原有 equipercentile 年际模型之上的增强；若用户想要更强波动/不同抖动系数，调 realistic-sim.js 顶部常量即可，不影响 engine/data。
