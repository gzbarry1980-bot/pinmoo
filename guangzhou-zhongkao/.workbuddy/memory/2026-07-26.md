# 2026-07-26 工作日志

## 生成昨日操作日志交接文档（移交 Codex）
- 任务：按用户要求，将 2026-07-25 的全部操作整理为结构化交接文档，移交对象 Codex。
- 产出：`E:\pinmoo\guangzhou-zhongkao\handover-2026-07-25.md`
- 内容结构：①接手必读（部署架构/禁区/沙箱限制/子页绝对路径铁律/当前版本）；②昨日 4 项操作（SPA→独立页真正上线 h、修复 null 报错 i、修复首页三模块消失 j、修复子页 /data 404 k），每项含任务描述/关键步骤/结果；③当前线上状态；④待办（付费后端未起、孤儿目录勿动等）。
- 数据来源：读取 `2026-07-25.md` 与 `MEMORY.md` 整理，未做任何线上改动。

## 开发并部署「序列号生成器」独立模块（serial-key）
- 需求：独立网站，三功能——10位唯一序列号生成（一键复制）、作废（输入/选择+二次确认，标记失效）、系统占位入口（链接 / ）。
- 工作流：⚡快速模式（TeamCreate 工具缺失，主理人直调 工程师→QA）。工程师新建 `serial-key/{index.html,styles.css,app.js}`（纯原生，localStorage key `zk_serial_keys`），未碰主程序禁区。QA 验证 IS_PASS: YES（元素匹配、2000序列号唯一性/长度/字符集测试 PASS、边界场景符合预期）。
- 部署：tar 管道把 `serial-key/` 传到生产 `pages1` release 子目录；健康检查 `/serial-key/` → 200。线上入口 https://zhongkao.pinmooconsulting.com/serial-key/
- 约束：模块独立，不依赖 engine.js/data，与主程序解耦。已写入 MEMORY.md「序列号生成器模块」节。

## 编写序列号生成器 API 集成文档（serial-key/API.md）
- 需求：提供完整开发文档，含 API 接口定义、请求/响应参数、认证方式、错误码列表，供与序列号后台系统集成对接。当前前端仅 localStorage、无后端，文档定位为「目标后端 API 契约」。
- 工作流：部分工作流，调度架构师(高见远)编写（先读 serial-key 源码确保一致）。产出 `serial-key/API.md`（8 章+2附录，含 Mermaid 流程图）。
- 文档要点：5 接口（POST generate / POST {code}/invalidate / GET list / GET {code} / POST validate）；认证预留 X-API-Key 或 Bearer Token；9 错误码（ERR_BAD_REQUEST/NOT_FOUND/ALREADY_INVALID/ALREADY_USED/DUPLICATE/UNAUTHENTICATED/FORBIDDEN/RATE_LIMITED/INTERNAL）；数据模型沿用 zk_serial_keys 记录结构；含前端 localStorage→API 迁移指引。
- 未改动任何代码文件，仅新建 API.md。
